{
  "name": "browser-ai-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "跨浏览器扩展 + Userscript，在 AI 网站上运行 Agent 操作",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:extension": "tsc --noEmit && rm -rf dist-extension && EXT_ENTRY=src/content/index.ts vite build --config vite.config.extension.ts && mv dist-extension/index.js dist-extension/content.js && EXT_ENTRY=src/background/index.ts vite build --config vite.config.extension.ts && mv dist-extension/index.js dist-extension/background.js && cp manifest.json dist-extension/",
    "preview": "vite preview"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/chrome": "^0.0.300",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vite-plugin-monkey": "^5.0.0"
  }
}
